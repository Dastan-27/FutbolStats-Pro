services:
  backend:
    build: .
    container_name: futbol-backend
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      # ✅ ERROR 5 Corregido: Apunta a db_futbol
      - postgresql://futbol_db_k4n2_user:dJlJ5wwNDf64kQLolQlN8SADEp4Ucue9@dpg-d8b03kugvqtc73a4gi80-a/futbol_db_k4n2 
    depends_on:
      - db_futbol

  db_futbol:
    image: postgres:15-alpine
    container_name: futbol-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: futbol_db
    ports:
      - "5432:5432"
    volumes:
      # ✅ ERROR 6 Corregido: Mapeo correcto para Postgres
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: